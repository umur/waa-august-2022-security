spring:
  application:
    name: waa-august-2022-security
  datasource:
    password: qwer
    url: jdbc:postgresql://localhost:5432/waa-aug-2022-lab6
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
server:
  port: 8080
