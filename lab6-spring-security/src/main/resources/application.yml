spring:
  application:
    name: lab6-spring-security
  datasource:
    password: postgrespw
    url: jdbc:postgresql://localhost:49154/lab6-spring-security
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
server:
  port: 8080
